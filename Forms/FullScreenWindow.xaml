<Window x:Class="Floatly.Forms.FullScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowState="Maximized"
        MouseMove="Window_MouseMove"
        Width="1280" Height="720">

    <!-- Dark elegant background -->
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0A0818" Offset="0"/>
            <GradientStop Color="#14122A" Offset="0.5"/>
            <GradientStop Color="#05040F" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>

        <!-- Video layer -->
        <MediaElement x:Name="Player"
                      LoadedBehavior="Manual"
                      Stretch="UniformToFill"/>
        <Rectangle Opacity="0.3">
            <Rectangle.Fill>
                <ImageBrush x:Name="Cover"/>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Lyrics overlay -->
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
              HorizontalScrollBarVisibility="Disabled"
              Padding="40">

            <ItemsControl x:Name="LyricItems">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                        Margin="0,0,0,30"
                        Padding="26"
                        CornerRadius="18"
                        Background="#552A6FFF"
                        BorderBrush="#884FA3FF"
                        BorderThickness="1"
                        HorizontalAlignment="Center"
                        Effect="{DynamicResource DropShadow}">

                            <!-- single-line, auto-shrink -->
                            <Viewbox Stretch="Uniform"
                             StretchDirection="DownOnly">
                                <TextBlock Text="{Binding Text}"
                                   FontSize="32"
                                   FontWeight="SemiBold"
                                   Foreground="#EEFFFFFF"
                                   TextWrapping="NoWrap"
                                   TextAlignment="Center"/>
                            </Viewbox>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>



        <!-- Bottom controls (auto hide) -->
        <Border x:Name="ControlBar"
                Height="90"
                VerticalAlignment="Bottom"
                Background="#66000000"
                Opacity="1"
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="" Width="40" Height="40" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="Button_PlayPause_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentPurple}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Border Background="White" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                    <Border.OpacityMask>
                        <ImageBrush ImageSource="/Assets/Images/icon-prev.png"/>
                    </Border.OpacityMask>
                </Border>
                <Button Content="" Width="40" Height="40" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="Button_PlayPause_Click" Grid.Column="1">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentPurple}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Border x:Name="Icon_PlayPause" Background="White" Width="20" Height="20" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                    <Border.OpacityMask>
                        <ImageBrush ImageSource="/Assets/Images/icon-pause.png"/>
                    </Border.OpacityMask>
                </Border>
                <TextBlock Grid.Column="2" Text="03:21"
           Foreground="White"
           VerticalAlignment="Center" x:Name="Label_CurrentTime"
                           FontSize="18" Margin="20,0,20,0"/>
                <Slider Grid.Column="3" VerticalAlignment="Center" Name="Slider_Progress" ValueChanged="Slider_Progress_ValueChanged"
PreviewMouseDown="Slider_Progress_PreviewMouseDown"
PreviewMouseUp="Slider_Progress_PreviewMouseUp">
                    <Slider.Resources>
                        <!-- Thumb: circular -->
                        <Style TargetType="Thumb">
                            <Setter Property="Width" Value="14"/>
                            <Setter Property="Height" Value="14"/>
                            <Setter Property="Margin" Value="0,-8,0,0"/>
                            <!-- move up a bit more -->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Thumb">
                                        <Ellipse Fill="Black"
Stroke="#6366F1"
StrokeThickness="2"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>


                        <!-- Slider: flat track -->
                        <Style TargetType="Slider">
                            <Setter Property="Foreground" Value="#6366F1"/>
                            <!-- progress bar color -->
                            <Setter Property="Background" Value="#1F2238"/>

                            <!-- background bar color -->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Slider">
                                        <Grid>
                                            <Track x:Name="PART_Track"
          Height="8"
          VerticalAlignment="Center">
                                                <!-- progress (left) -->
                                                <Track.DecreaseRepeatButton>
                                                    <RepeatButton Background="{TemplateBinding Foreground}"
                         BorderThickness="0">
                                                        <RepeatButton.Template>
                                                            <ControlTemplate TargetType="RepeatButton">
                                                                <Border Background="{TemplateBinding Background}"
                               Height="6"
                               VerticalAlignment="Center"/>
                                                            </ControlTemplate>
                                                        </RepeatButton.Template>
                                                    </RepeatButton>
                                                </Track.DecreaseRepeatButton>
                                                <!-- background (right) -->
                                                <Track.IncreaseRepeatButton>
                                                    <RepeatButton Background="{TemplateBinding Background}"
                         BorderThickness="0">
                                                        <RepeatButton.Template>
                                                            <ControlTemplate TargetType="RepeatButton">
                                                                <Border Background="{TemplateBinding Background}"
                               Height="6"
                               VerticalAlignment="Center"/>
                                                            </ControlTemplate>
                                                        </RepeatButton.Template>
                                                    </RepeatButton>
                                                </Track.IncreaseRepeatButton>
                                                <!-- thumb -->
                                                <Track.Thumb>
                                                    <Thumb/>
                                                </Track.Thumb>
                                            </Track>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Slider.Resources>
                </Slider>
                <TextBlock Grid.Column="4" Text="03:21"
                           Foreground="White"
                           VerticalAlignment="Center" x:Name="Label_TotalTime"
                           FontSize="18" Margin="20,0,20,0"/>
            </Grid>
        </Border>

    </Grid>
</Window>
